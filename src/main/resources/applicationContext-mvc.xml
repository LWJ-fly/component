<?xml version="1.0" encoding="utf-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.2.xsd">
    <!-- SpriongMVC只扫描控制器 [一定要禁用默认规则才行]-->
    <context:component-scan base-package="online.wenmeng.controller" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>
    <!-- 配置视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/page/"/>
        <property name="suffix" value=".html"/>
    </bean>
    <!-- 配置文件上传解析器 -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="utf-8"/>
        <!-- #{}为表达式：可以计算其中的简单属性。20M以内就可以 -->
        <property name="maxUploadSize" value="#{1024*1024*20}"/>
    </bean>

    <!--  那些文件不进行拦截  -->
    <mvc:resources mapping="/css/" location="/css/**"/>
    <mvc:resources mapping="/img/" location="/img/**"/>
    <mvc:resources mapping="/js/" location="/js/**"/>
    <mvc:resources mapping="html" location="/**.html"/>

    <!-- 扫描静态资源 -->
    <mvc:default-servlet-handler/>
    <!-- 扫描动态资源 -->
    <mvc:annotation-driven/>

    <!-- 配置异常处理器 -->
<!--    <bean id="sysExceptionResolver" class="online.wenmeng.exception.sysExceptionResolver"/>-->
    <!-- 配置拦截器 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 拦截的具体方法  拦截所有-->
            <mvc:mapping path="/**"/>
            <!-- 配置拦截器 -->
            <bean class="online.wenmeng.interceptor.MyInterceptor1" />
            <!-- 不需要拦截的方法 -->
<!--            <mvc:exclude-mapping path=""/>-->
        </mvc:interceptor>
    </mvc:interceptors>
</beans>
